import { Inter } from "next/font/google";
import { Abel } from "next/font/google";
import { Crimson_Text } from "next/font/google";
import { Oswald } from "next/font/google";
import { Playfair_Display } from "next/font/google";
import { Open_Sans } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });
export const abel = Abel({ subsets: ["latin"], weight: "400" });
export const crimsonText = Crimson_Text({ subsets:["latin"], weight: "400" });
export const playfairDisplay = Playfair_Display({ subsets:["latin"], weight: "600" });
export const openSans = Open_Sans({subsets:["latin"], weight: "400"});
export const oswald = Oswald({subsets:["latin"], weight: "400"});